import java.util.ArrayList;
import java.util.List;

// Asif - This class will have everything a user may use. The design pattern of ToDo class can help in this case. There will be another table having User name and password. Sql queries will be handled by ToDoqueries. 
// This class will have the ability to hold all the ids of tasks the user itself has. So that each time we store a user means storing the id of the task that specific user has listed on Todo List.
// List can be used to hold the info's. Every possible function ( Including setters and getters ) should be written with appropriate parameters. 
// Depending on Sql queries and gui, the code might be changed. So User class in general having everything a user can use is preferred. 

public class User {
	
	//===========================================================================
	// FIELDS
	//===========================================================================
	
	
	
	//===========================================================================
	// CONSTRUCTOR
	//===========================================================================
	
	
	
	
	
	//===========================================================================
	// METHODS
	//===========================================================================
	// GETTERS
	//---------------------------------------------------------------------------
	
	
	
	 //---------------------------------------------------------------------------
	 // SETTERS
	 //---------------------------------------------------------------------------
	
	
	//---------------------------------------------------------------------------
	// OTHER METHODS
	//---------------------------------------------------------------------------
}
